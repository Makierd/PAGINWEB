/* =========================================================
   MAIN.JS — Interacciones de la pagina principal
   ========================================================= */

/* ---------- Modal del poster: ampliar / cerrar ---------- */
function togglePosterModal() {
    const modal = document.getElementById('poster-modal');
    modal.classList.toggle('active');
}
